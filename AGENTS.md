# EthioMLS

## Project Purpose
EthioMLS is an agent-facing MLS workspace for Ethiopian real estate professionals.

## Current Architecture
- Next.js App Router application using React, TypeScript, and Tailwind CSS.
- Core listing UI lives under `app/`.
- Shared listing form lives in `components/PropertyForm.tsx`.
- Listing persistence is now backed by Supabase/Postgres through server-side REST helpers:
  - `lib/supabase.ts` builds Supabase REST requests from environment variables.
  - `lib/listings.ts` maps Supabase rows to the existing app-facing `Property` shape.
- API routes handle writes:
  - `POST /api/listings` creates listings.
  - `PUT /api/listings/[id]` updates listings.
  - `DELETE /api/listings/[id]` deletes listings.
- Reads are routed through `lib/listings.ts`:
  - Home promoted listings.
  - Browse listings.
  - Listing details.
  - Edit listing.
  - Photo placeholder page.
  - My Listings.
- `data/properties.ts` remains as a read fallback if Supabase env vars are absent or reads fail.

## Features Completed
- Browse listings.
- Search/filter listings on `/listings`.
- View listing details.
- Add listing with Supabase persistence.
- Edit listing with Supabase persistence.
- Delete listing with Supabase persistence after confirmation.
- My Listings filtered by owner.
- Ownership-based UI and edit/delete guards using mock agent ID.
- Non-owner request showing success state.
- Non-owner save listing success state.
- Manage Photos placeholder page.
- Add Listing no longer requires image upload.
- Demo-polished navbar fallback routes for deferred pages.
- Styled not-found and access-denied states.

## Supabase Integration Status
- Supabase/Postgres is the active persistence layer for listings.
- The app uses Supabase REST endpoints directly via `fetch`; no Supabase client package is installed.
- Read helpers fall back to mock listing data on failure.
- Write operations do not fall back to mock data. Create/update/delete require Supabase env vars and a working `public.listings` table.
- `listing_id` is human-readable and unique at the database level.
- Internal route identity remains `id`, not `listing_id`.
- New `listing_id` values are generated by reading existing `MLS-####` values and incrementing the highest number.
- Current generation is acceptable for MVP but is not race-safe for simultaneous creates.

## Environment Variables Required
Set these locally in `.env.local` and in Vercel project settings:

```env
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
```

Important:
- `NEXT_PUBLIC_SUPABASE_URL` must be the project root URL only, not `/rest/v1`.
- Keep `SUPABASE_SERVICE_ROLE_KEY` server-only. Do not expose it in client code.

## Database Schema Overview
Schema file: `supabase/listings.sql`

Table: `public.listings`

Important columns:
- `id text primary key` - internal app/database identity.
- `listing_id text not null unique` - human-readable MLS-style ID.
- `title text not null`
- `price text not null`
- `location text not null`
- `property_type text not null`
- `status text not null`
- `verified boolean not null default false`
- `bedrooms integer not null`
- `bathrooms integer not null`
- `agent text not null`
- `updated_at_label text not null`
- `approval_status text not null`
- `description text not null`
- `image text not null`
- `owner_id text not null`
- `created_at timestamptz`
- `updated_at timestamptz`

Indexes:
- `owner_id`
- `status`
- `property_type`

Trigger:
- Updates `updated_at` automatically before row updates.

Seed data:
- `MLS-1001`, owner `agent-1`
- `MLS-1002`, owner `agent-2`
- `MLS-1003`, owner `agent-1`

## Deployment Status
- MVP UI is working in deployment.
- Supabase project has been created by the user.
- Environment variables have been configured by the user.
- `supabase/listings.sql` should be run in the Supabase SQL Editor for any new Supabase project/environment.
- Vercel deployment must include the same Supabase env vars:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
- After Vercel env vars change, redeploy the app.

## Authentication and Ownership
- Authentication is not implemented yet.
- Mock current agent remains `agent-1`.
- Ownership rules:
  - Owners can edit/delete/manage photos.
  - Non-owners can request showings and save listings.
- Replace all mock `currentAgentId = "agent-1"` usage with authenticated user data when auth is implemented.

## Remaining MVP Tasks Ranked by Priority
1. Make MLS listing ID generation database-owned with a Postgres sequence or RPC function.
2. Add real authentication and replace mock `agent-1` ownership checks.
3. Add Row Level Security policies after auth exists.
4. Replace hard delete with soft delete if listing recovery/audit history matters.
5. Implement real photo uploads and photo management.
6. Implement showing-request persistence and/or email notifications.
7. Replace demo navbar fallback routes with real Clients, Financial, Subscription, Billing, and Settings pages.
8. Replace static dashboard metrics with real Supabase-derived counts.
9. Update app metadata from default Next.js values.
10. Consider switching raw `<img>` tags to Next `<Image />` if image optimization becomes important.

## Known Limitations and Technical Debt
- App-side `listing_id` generation is not concurrency-safe. The database unique constraint protects duplicates, but one simultaneous create could fail.
- Create/update/delete use Supabase service role through server routes. This is acceptable for the current mocked-auth MVP but should be revisited with real auth and RLS.
- Reads silently fall back to mock data if Supabase fails, which helps local resilience but can hide production configuration issues.
- Delete is currently a hard delete.
- Add Listing uses a fallback image because image uploads are not implemented.
- Edit Listing uses image URL, not upload/photo management.
- Request Showing and Save Listing only show local success states.
- Admin dashboard remains hardcoded and outside the current active MVP path.
- Home dashboard metrics are static.
- No automated tests exist yet.

## Recommended First Task for Next Session
Implement database-owned MLS ID generation.

Goal:
- Move `listing_id` generation into Supabase using a sequence or SQL function.
- Keep format like `MLS-1004`.
- Preserve existing seeded listings.
- Remove race-prone app-side highest-number lookup from `lib/listings.ts`.

Suggested approach:
- Add a Supabase SQL migration that creates a sequence starting after the current max MLS number.
- Add a SQL function/RPC to generate the next `listing_id`, or a column default/trigger.
- Update create listing flow to let Supabase assign `listing_id` or call the RPC before insert.
- Test creating several listings back to back.

## Manual Testing Checklist
Run these after Supabase env vars are configured and `supabase/listings.sql` has been applied.

### Read
- Open `/listings`.
- Expected: Supabase seeded listings appear.
- Open `/listings/1`.
- Expected: seeded detail page loads with `MLS-1001`.
- Open `/my-listings`.
- Expected: only `agent-1` listings appear.
- Open `/listings/999`.
- Expected: styled not-found state.

### Create
- Open `/add-listing`.
- Submit a valid listing without selecting images.
- Expected: success message appears.
- Open `/my-listings`.
- Expected: new listing appears.
- Refresh `/my-listings`.
- Expected: new listing remains.
- Check Supabase Table Editor.
- Expected: new row has `owner_id = agent-1`, `approval_status = Pending`, `verified = false`, and short `listing_id` like `MLS-1004`.

### Update
- From `/my-listings`, open an owned listing edit page.
- Change title, price, bedrooms, bathrooms, and description.
- Save.
- Expected: success message appears.
- Open the listing detail page and refresh.
- Expected: edited values persist.

### Delete
- Open an owned listing detail page.
- Click `Delete Listing`, then cancel.
- Expected: listing remains.
- Click `Delete Listing`, then confirm.
- Expected: redirect to `/my-listings`.
- Expected: deleted listing is gone from UI and Supabase.

### Ownership
- Open `/listings/2`.
- Expected: non-owner actions are shown.
- Open `/listings/2/edit`.
- Expected: access denied.
- Open `/listings/2/photos`.
- Expected: access denied.

### Listing IDs
- Add two listings back to back.
- Expected: IDs increment, such as `MLS-1004`, then `MLS-1005`.
- Confirm database `id` remains the internal primary key and `listing_id` is the display MLS code.

## Coding Rules
- Preserve EthioMLS styling.
- Prefer reusable components.
- Do not modify unrelated files.
- Add TODO comments for future work.
- Do not introduce auth, billing, payments, emails, or image uploads unless explicitly requested.
- Keep MVP changes narrow and verify with `npm run lint`.

## Next Session Context
- User stopped work for today and asked for this `AGENTS.md` update only.
- Do not assume application code changes are desired at the start of the next session.
- Current app has Supabase listing persistence for CRUD.
- Supabase REST URL bug was diagnosed: `NEXT_PUBLIC_SUPABASE_URL` must not include `/rest/v1`.
- User successfully tested adding a new home after Supabase setup.
- User asked about shorter `listing_id`; current code now generates the next `MLS-####` by scanning existing `listing_id` values.
- Recommended next engineering task is to move MLS ID generation into Supabase/Postgres for concurrency safety.
- Build command was previously blocked by an intermittent Windows sandbox `spawn setup refresh` error; lint passed with only existing `<img>` warnings.
